<h1>編集画面</h1>
<%= form_with model: @list do |f| %>
  <p>タイトル</p>
  <%= f.text_field :title %>
  <p>内容</p>
  <%= f.text_area :body %>

  <h4>画像</h4>
  <%= f.file_field :image, accept: "image/*" %>
  
  <%= f.submit '投稿' %>

  <%= f.submit "編集を終える" %>
<% end %>
